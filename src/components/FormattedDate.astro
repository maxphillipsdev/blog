---
const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="text-gray-11 italic">
  {
    date.toLocaleDateString("en-us", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
