---
import { SITE_TITLE, SITE_DESCRIPTION } from "../consts";
import Layout from "../layouts/Base.astro";

const response = await fetch("https://dev.to/api/users/473140");
const data = await response.json();

export const prerender = true;
---

<Layout
  title={SITE_TITLE}
  description={SITE_DESCRIPTION}
  heroImage={"/placeholder-about.png"}
>
  <article
    class="prose prose-lg prose-slate flex max-w-none flex-col items-center text-center font-light tracking-tight"
  >
    <h1 class="tracking-tight">Welcome! 👋</h1>
    <img src={"/placeholder-about.png"} alt="" class="rounded-xl shadow-lg" />
    <p>
      {data.summary}
    </p>
  </article>
</Layout>
